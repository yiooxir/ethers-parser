{
  "name": "parser",
  "version": "1.1.0",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "test": "mocha --require ts-node/register -r tsconfig-paths/register --timeout 5000 --ui bdd __tests__/*.spec.ts",
    "build": "npm test && rimraf build && tsc && echo \"=== Build complete ===\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ethers": "^5.5.3"
  },
  "devDependencies": {
    "@types/mocha": "^9.0.0",
    "@types/node": "^17.0.8",
    "chai": "^4.3.4",
    "mocha": "^9.1.3",
    "rimraf": "^3.0.2",
    "sinon": "^12.0.1",
    "ts-mocha": "^8.0.0",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  }
}
